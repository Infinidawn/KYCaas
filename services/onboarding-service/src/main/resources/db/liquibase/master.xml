<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="
          http://www.liquibase.org/xml/ns/dbchangelog
          https://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.24.xsd">

    <!-- ========================================================= -->
    <!-- Ensure schema exists                                      -->
    <!-- ========================================================= -->
    <changeSet id="2025-09-30-create-onboarding-schema" author="btc-kyc">
        <sql>CREATE SCHEMA IF NOT EXISTS onboarding;</sql>
    </changeSet>

    <!-- ========================================================= -->
    <!-- KYC Session table                                         -->
    <!-- ========================================================= -->
    <changeSet id="2025-09-30-create-kyc-session" author="btc-kyc">
        <createTable tableName="kyc_session" schemaName="onboarding">
            <column name="id" type="UUID">
                <constraints primaryKey="true" nullable="false"/>
            </column>
            <column name="tenant_id" type="UUID">
                <constraints nullable="false"/>
            </column>
            <column name="channel" type="VARCHAR(32)">
                <constraints nullable="false"/>
            </column>
            <column name="phone" type="VARCHAR(32)"/>
            <column name="state" type="VARCHAR(32)">
                <constraints nullable="false"/>
            </column>
            <column name="created_at" type="TIMESTAMPTZ" defaultValueComputed="CURRENT_TIMESTAMP">
                <constraints nullable="false"/>
            </column>
        </createTable>

        <createIndex indexName="idx_session_tenant" tableName="kyc_session" schemaName="onboarding">
            <column name="tenant_id"/>
        </createIndex>

        <rollback>
            <dropTable tableName="kyc_session" schemaName="onboarding"/>
        </rollback>
    </changeSet>

    <!-- ========================================================= -->
    <!-- KYC Decision table                                        -->
    <!-- ========================================================= -->
    <changeSet id="2025-09-30-create-kyc-decision" author="btc-kyc">
        <createTable tableName="kyc_decision" schemaName="onboarding">
            <column name="id" type="UUID">
                <constraints primaryKey="true" nullable="false"/>
            </column>
            <column name="session_id" type="UUID">
                <constraints nullable="false"/>
            </column>
            <column name="status" type="VARCHAR(32)">
                <constraints nullable="false"/>
            </column>
            <column name="reasons" type="TEXT"/>
            <column name="created_at" type="TIMESTAMPTZ" defaultValueComputed="CURRENT_TIMESTAMP">
                <constraints nullable="false"/>
            </column>
        </createTable>

        <addForeignKeyConstraint
                baseTableName="kyc_decision"
                baseColumnNames="session_id"
                baseTableSchemaName="onboarding"
                referencedTableName="kyc_session"
                referencedColumnNames="id"
                referencedTableSchemaName="onboarding"
                constraintName="fk_decision_session"
                onDelete="CASCADE"/>

        <createIndex indexName="idx_decision_session" tableName="kyc_decision" schemaName="onboarding">
            <column name="session_id"/>
        </createIndex>

        <rollback>
            <dropTable tableName="kyc_decision" schemaName="onboarding"/>
        </rollback>
    </changeSet>

    <!-- ========================================================= -->
    <!-- KYC Stage Log table (new)                                 -->
    <!-- ========================================================= -->
    <changeSet id="2025-09-30-create-kyc-stage-log" author="btc-kyc">
        <createTable tableName="kyc_stage_log" schemaName="onboarding">
            <column name="id" type="UUID">
                <constraints primaryKey="true" nullable="false"/>
            </column>
            <column name="session_id" type="UUID">
                <constraints nullable="false"/>
            </column>
            <column name="event" type="VARCHAR(64)">
                <constraints nullable="false"/>
            </column>
            <column name="details" type="TEXT"/>
            <column name="created_at" type="TIMESTAMPTZ" defaultValueComputed="CURRENT_TIMESTAMP">
                <constraints nullable="false"/>
            </column>
        </createTable>

        <addForeignKeyConstraint
                baseTableName="kyc_stage_log"
                baseColumnNames="session_id"
                baseTableSchemaName="onboarding"
                referencedTableName="kyc_session"
                referencedColumnNames="id"
                referencedTableSchemaName="onboarding"
                constraintName="fk_stage_log_session"
                onDelete="CASCADE"/>

        <createIndex indexName="idx_stage_log_session" tableName="kyc_stage_log" schemaName="onboarding">
            <column name="session_id"/>
        </createIndex>

        <createIndex indexName="idx_stage_log_created_at" tableName="kyc_stage_log" schemaName="onboarding">
            <column name="created_at"/>
        </createIndex>

        <rollback>
            <dropTable tableName="kyc_stage_log" schemaName="onboarding"/>
        </rollback>
    </changeSet>

</databaseChangeLog>
